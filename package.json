{
    "name": "chibi-office-ai",
    "version": "0.1.0",
    "private": true,
    "description": "Chibi Office AI — AI-powered office simulation for agent team orchestration",
    "scripts": {
        "dev": "concurrently \"npm run dev:backend\" \"npm run dev:renderer\"",
        "dev:backend": "cd apps/backend && python3 main.py",
        "dev:renderer": "cd apps/renderer && npm run dev",
        "dev:electron": "cd apps/desktop && npm run dev",
        "install:all": "npm install && cd apps/renderer && npm install && cd ../desktop && npm install",
        "build:renderer": "cd apps/renderer && npm run build",
        "package": "npm run build:renderer && cd apps/desktop && npm run package"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}